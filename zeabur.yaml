name: grammar-bot

services:
  - name: bot
    type: web
    buildCommand: pip install -r requirements.txt
    startCommand: python3 main.py
    env:
      - name: OPENROUTER_API_KEY
      - name: BOT_TOKEN
    ports:
      - 8080
